# Personal Expense Tracker — Planning Document

> وثيقة التخطيط الشاملة: Product + UX + Architecture + Database Planning.
> هذه الوثيقة **مرجع تخطيطي فقط** — لا تحتوي على أي كود، ولا تُلغي أي قرار في ملف الدستور `personal_expense_tracker_project.md`؛ بل تفصّل ما تركه مفتوحًا.
> أي قرار غير محسوم يُعرض بصيغة: **Decision / Options / Recommendation / Reason**.

---

## A. Product Definition

### A.1 المشكلة الأساسية

معظم تطبيقات تسجيل المصروفات إما معقدة (محاسبة كاملة، حسابات بنكية، استثمارات) أو تطلب حسابًا واتصالًا بالإنترنت، أو تربط المستخدم بمفهوم «الحسابات» الذي لا يحتاجه الشخص العادي.

المستخدم المستهدف يريد سؤالين فقط في أي لحظة:

1. **كم صرفت؟**
2. **كم بقي لي؟**

### A.2 الحل المقترح

تطبيق Flutter خفيف، سريع، يعمل Offline بالكامل، يخزّن البيانات محليًا فقط، ويبنى حول مفهوم واحد مركزي:

> **Available Money** — المال المتاح للإنفاق، بغض النظر عن مكانه (نقدًا، بطاقة، محفظة هاتف).

التطبيق يسجل «ماذا صرفت» ويساعد على «فهم ما تبقى» عبر ميزانية اختيارية بدورة مرنة وقوائم تسوق مرتبطة مباشرة بالمصاريف.

### A.3 حدود المنتج

| هو | ليس |
|---|---|
| Private, offline-first spending tracker | بنك أو مؤسسة مالية |
| Optional budget cycles with flexible start day | ERP محاسبي |
| Shopping planning tied to expenses | Notes app أو Task manager عام |
| Simple, understandable statistics | منصة استثمار أو تحليل مالي متقدم |

المعادلة الحاكمة لأي قرار مستقبلي: **Record → Understand → Decide** (من الدستور، القسم 26).

---

## B. MVP Scope

### B.1 داخل MVP

| الميزة | ملاحظات |
|---|---|
| Add / Edit / Delete expense | Name + Amount إلزاميان؛ Quantity/Unit Price/Category/Note اختيارية؛ المبلغ المحسوب (كمية × سعر) يُثبَّت تلقائيًا في الحقل ما لم يُدخل يدويًا |
| مجموعات الشراء (نفس الفاتورة) | بنود تُسجل معًا وتُعرض في بطاقة واحدة بإجمالي مشتق؛ ميزة أُقرّت بعد MVP الأساسي بناءً على طلب المالك |
| حساب Amount تلقائيًا من Quantity × Unit Price | مع إمكانية الإدخال المباشر للمبلغ النهائي |
| Budget Mode اختياري | initial amount + start day (أي يوم من الشهر) + carry-over اختياري |
| حسابات الدورة | Spent / Remaining / الأيام المتبقية / Daily indicator (إرشادي) |
| Shopping Lists | عناصر بأسعار تقديرية، Estimated total، Purchased state، تحويل إلى Expense فعلي |
| Categories | 7 افتراضية + إضافة من المستخدم، اختيارية دائمًا |
| Statistics بسيطة | Today / Week / Month / Cycle / Custom range / Average daily / By category / Top days |
| Localization | العربية (RTL) + الإنجليزية + الفرنسية (LTR) |
| Currency configurable | مستقلة عن اللغة، integer minor units |
| Soft-delete + UUIDs + timestamps | لجاهزية المزامنة المستقبلية |

**نموذج الوضعين:** التطبيق يعمل بوضعين ذهنيين فوق نفس قاعدة البيانات — **الوضع 1 «تسجيل فقط»** (`budget_enabled = false`): تسجيل وجمع بلا ميزانية؛ **الوضع 2 «مبلغ يُخصم منه»** (`budget_enabled = true`): مبلغ دوري + دورة مرنة. الانتقال بين الوضعين إعداد واحد، والوضع 1 هو الافتراضي بعد أول تشغيل.

### B.2 خارج MVP نهائيًا (إلا بموافقة صريحة)

Bank integration، استيراد معاملات بنكية تلقائي، Investment tracking، Debt management، Business accounting، User accounts، Cloud sync، Social features، تحليلات مالية متقدمة، ميزات متكررة معقدة (recurring)، Notes/To-Do عام.

### B.3 مؤجل (Deferred — قابل للإضافة لاحقًا دون إعادة بناء)

| الميزة | سبب التأجيل |
|---|---|
| Export/Import (JSON backup, CSV) | مطلوب لتطبيق privacy-first لكنه غير ضروري للإطلاق؛ الـ schema يدعمه |
| App lock (PIN / biometric) | تحسين خصوصية، وليس شرطًا لوظيفة التطبيق |
| Widgets / Quick tile للإضافة السريعة | تحسين Speed لاحقًا بعد استقرار تدفق الإضافة |
| Cloud Sync | تصميم جاهز (قسم I) لكن التنفيذ مؤجل بالكامل |
| Recurring expenses بسيطة | تحتاج دراسة UX منفصلة |

---

## C. User Flows

### C.1 First Launch

1. فتح التطبيق لأول مرة → Home بشاشة ترحيب خفيفة (قيمة فارغة: «لا مصروفات بعد» + زر Add Expense بارز).
2. الشاشة الأولى تعرض **اختيار اللغة صراحةً** (العربية/English/Français — لغة النظام مجرد اختيار مسبق لا قرار صامت) **قبل اختيار العملة**.
3. العملة: يختارها المستخدم صراحةً في نفس الشاشة — خطوة واحدة غير قابلة للتخطي؛ **لا استنتاج من اللغة أو المنطقة إطلاقًا** (K.1 محسوم).
4. Budget Mode **غير مفعّل** افتراضيًا. Home يعرض إجمالي مصروفات اليوم/الشهر فقط، مع دعوة خفيفة «فعّل الميزانية».
5. لا تسجيل حساب، لا أذونات، لا اتصال بالشبكة.

### C.2 Add Expense (التدفق الأهم — Quick Add)

1. الضغط على زر `+` (FAB) → شاشة/نموذج Add Expense بحقلي Name وAmount في الأعلى مباشرة، لوحة أرقام مفتوحة على Amount.
2. `Save` → حفظ فوري والعودة إلى Home، مع Toast/إظهار خفيف للمصروف المضاف وزر Undo.
3. Category افتراضيًا «غير مصنّف» — **لا تفرض اختيارًا أبدًا**.
4. اختياري (مخفي افتراضيًا خلف «تفاصيل» أو قابلة للطي): Quantity + Unit Price → يُحسب Amount تلقائيًا (ويبقى قابلًا للتعديل اليدوي)، Category، Note، Date/Time (افتراضيًا الآن).
5. قاعدة التعديل اليدوي: إذا عدّل المستخدم Amount يدويًا بعد حسابه من Quantity × Unit Price، فإن Amount المدخل هو القيمة النهائية (يُحفظ quantity وunit_price كما هما للسجل التاريخي).

### C.3 Edit Expense

1. من Expenses (أو من آخر المصروفات في Home) → الضغط على المصروف → شاشة التعديل بنفس نموذج الإضافة.
2. `created_at` لا يتغير؛ `updated_at` يتحدث.
3. الحذف من شاشة التعديل أو بسحب العنصر في القائمة (Swipe) مع تأكيد.

### C.4 Delete Expense

1. Swipe-to-delete أو زر داخل شاشة التعديل → حوار تأكيد واحد يوضح المبلغ.
2. الحذف **Soft delete** (`deleted_at` يُملأ) — لا حذف فيزيائي.
3. تُعاد حسابات Home/Budget فورًا بعد الحذف.

### C.5 Create Budget

1. من Home (بطاقة «Budget») أو Settings → Budget.
2. المستخدم يدخل: Initial amount، Start day (1–28، مع ملاحظة أن 29/30/31 يُعالج كما في G.3)، Carry-over (toggle).
3. عند التفعيل: أول دورة = **الفترة الحالية التي تحتوي اليوم، بأثر رجعي** (اليوم 8 سبتمبر وstart day = 1 → الدورة 1–30 سبتمبر، والمصروفات المسجلة منذ 1 سبتمبر تُحتسب). شاشة التفعيل تعرض ملخصًا شفافًا: «المبلغ 1500 − المصروف منذ بداية الفترة 220 = المتبقي 1280». خيار ثانوي «ابدأ من اليوم» متاح لمن يريد صفحة نظيفة (دورة جزئية حتى نهاية الفترة الحالية — K.11).
4. إلغاء تفعيل Budget Mode ممكن في أي وقت؛ الدورات المغلقة تبقى محفوظة كلقطات تاريخية (للإحصاءات) لكن لا تُحسب في Home. إعادة التفعيل لاحقًا تُعامل معاملة أول تفعيل (النقطة 3).

### C.6 Start New Budget Cycle — محسوم (Lazy Materialization)

القاعدة المركزية: **الدورة الحالية تُحسب ولا تُخزَّن** — تُشتق لحظيًا من `start_day + اليوم`. صف الدورة يُنشأ فقط في حالتين: عند تفعيل الميزانية (C.5)، وعند إغلاق دورة منتهية.

- عند كل فتح للتطبيق: هل توجد دورة نشطة تحتوي اليوم؟
  - نعم → لا يُنشأ شيء؛ الحسابات حية من جدول expenses.
  - لا → تُغلق آخر دورة فعلية وفق قواعد الإغلاق (G.5)، ثم **تُنشأ دورة الفترة الحالية مباشرة**. لا تُنشأ دورات تاريخية فارغة للفترات التي لم يُفتح فيها التطبيق (قاعدة الترحيل عبر الفجوات: G.5).
- المستخدم لا يرى «إدارة دورات» إطلاقًا؛ يرى فقط التواريخ في بطاقة Budget.
- زر «ابدأ دورة جديدة الآن» إجراء متقدم في Budget screen: يغلق الجارية بأن **الأمس آخر أيامها** (تُعاد كتابة `end_date` ليتطابق مع الدفاتر)، وتبدأ الدورة الجديدة **اليوم** كنافذة جزئية حتى نهاية الفترة الطبيعية — مصروفات اليوم تُنسب للدورة الحية (مرة واحدة عبر الدفاتر كلها). دورة بدأت اليوم تُحبَط (tombstone) لا تُغلق. **الثابت المحوري:** الدورة المفتوحة تحتوي اليوم دائمًا — أي مخالفة تسبب إغلاقًا وإعادة إنشاء عند كل فتح وإعادة نسبة كل التاريخ للدورة الجديدة (سالب). وكذلك إعادة تفعيل الميزانية وهي مفتوحة: تُنهى القديمة بهذه القاعدة قبل إنشاء أول دورة (لا دورتان مفتوحتان أبدًا).

### C.7 Carry Over

1. Carry-over إعداد مخزّن **لكل دورة** (يُورَّث من الدورة السابقة، قابل للتغيير في Budget screen).
2. عند إنشاء الدورة الجديدة: إذا كان carry-over مفعّلًا → متاح الدورة الجديدة = `default_budget_amount + final_remaining(previous)` (انظر G.5). إذا معطّل → المبلغ الافتراضي فقط.
3. يظهر في بطاقة الدورة الجديدة سطر «+ Carry-over من الدورة السابقة: 300».
4. Carry-over السالب (محسوم): يُرحَّل كما هو إذا كان الترحيل مفعّلًا (1500 − 200 = 1300 بدايةً)؛ ومع ترحيل معطّل تبدأ الدورة بالمبلغ الافتراضي نظيفة. لا اقتطاع صامت، ولا حجب للإنفاق أبدًا.

### C.8 Create Shopping List

1. تبويب Lists → زر `+` → إدخال اسم القائمة فقط (مثلاً «Weekly Shopping») → تُنشأ فورًا.
2. داخل القائمة: إضافة عنصر = Name + Estimated price (اختياري) + Quantity (افتراضي 1) + Note (اختياري).
3. تُعرض فوق القائمة: **Estimated total** + (إذا Budget Mode مفعّل) مقارنة بصرية خفيفة مع Remaining (مثلاً «~25% من المتبقي»).
4. القائمة المكتملة (كل عناصرها Purchased) لا تُحذف ولا تُؤرشف تلقائيًا — تبقى ظاهرة، والأرشفة/الحذف **يدويان** بيد المستخدم (K.7 محسوم). المؤرشفة تنتقل إلى قسم «مؤرشفة» (`archived_at` منفصل تمامًا عن `deleted_at`).

### C.9 Purchase Shopping-List Item

1. Checkbox بجانب العنصر أو Swipe → إذا لم يوجد actual price، يظهر فورًا حوار صغير: «Actual price؟» مع اقتراح السعر التقديري مملوءًا مسبقًا.
2. تأكيد → العنصر يصبح Purchased، `purchased_at` يُسجل.

### C.10 Convert Estimated Price to Actual Expense

1. يدمج مع C.9: عند تأكيد الشراء يُسأل المستخدم سؤال واحد فقط: «تسجيل كمصروف الآن؟» (افتراضيًا نعم).
2. عند التأكيد: يُنشأ Expense بـ:
   - `amount` = **Actual price** (ليس التقديري).
   - `name` = اسم العنصر، `quantity` = كمية العنصر، `category` = فئة القائمة إن حُدّدت، `note` يذكر أنه من القائمة.
   - `created_at` = وقت الشراء/التحويل.
3. العنصر يخزّن `expense_id` (ربط تتبّعي) ويتحول حالته إلى Purchased. لا يُحتسب عنصر Purchased في Estimated total المتبقي.
4. لا يوجد Bulk conversion في MVP — التحويل عنصرًا بعنصر فقط، لأن كل عنصر يحتاج سعره الفعلي، وبعض العناصر قد لا تكون قد شُريت (K.4 محسوم).

### C.11 View Statistics

1. تبويب Statistics → افتراضيًا «This month» (أو الدورة الحالية إن كان Budget مفعّلًا).
2. تبويبات/شرائح سريعة: Today / Week / Month / Cycle + زر Custom range (منتقي تاريخين).
3. المحتوى: إجمالي المصروف، Average daily spending، مصروف حسب Category (قائمة أشرطة بسيطة)، أعلى أيام الصرف (أعلى 5).
4. كل قيمة قابلة للضغط → تفتح Expenses مفلترة حسب المعيار نفسه.

---

## D. Screen Map

**Decision:** كم شاشة في MVP؟
- **Options:** (1) 7 شاشات منفصلة كما وردت في الطلب. (2) 4 تبويبات + شاشات ثانوية.
- **Recommendation:** (2) Bottom Navigation Bar بأربعة تبويبات: **Home / Expenses / Lists / Stats**، و**Budget** شاشة تُفتح من Home وSettings، و**Settings** من أيقونة في Home (أو AppBar). Add Expense نافذة/شاشة تُفتح من FAB.
- **Reason:** 6 تبويبات تكتّف القاع وتضع Budget وSettings (استخدام نادر) في نفس مستوى Home (استخدام يومي)؛ الدمج يحافظ على السرعة والبساطة.

```text
Home (tab 1)
├── FAB: Add Expense (modal/sheet أو شاشة)
├── بطاقة Budget (إن كان Budget مفعّلًا: Remaining / Spent / Daily indicator / Cycle dates)
│   └── → Budget screen (إعداد/تعديل/إنهاء/بدء دورة)
├── Recent expenses (آخر 5) → Expense detail/edit
├── أيقونة Settings → Settings screen
│   ├── Language
│   ├── Currency
│   ├── Budget settings → Budget screen
│   ├── Privacy (شرح سياسة الخصوصية — نص فقط)
│   └── Data management (Export/Import لاحقًا؛ في MVP: مسح كل البيانات بتأكيد مزدوج)
│
Expenses (tab 2)
├── قائمة زمنية مجمّعة حسب اليوم (مع مجموع كل يوم)
├── Search + فلتر فترة + فلتر Category
└── → Expense detail/edit → Delete (soft)
│
Lists (tab 3)
├── قائمة Shopping lists (نشطة + مؤرشفة)
└── → Shopping list detail
    ├── إضافة/تعديل/حذف عناصر
    ├── Estimated total + مقارنة مع Remaining
    └── Purchase item → Actual price dialog → Convert to expense
│
Stats (tab 4)
├── شرائح: Today / Week / Month / Cycle + Custom range
├── Total / Average daily / By category / Top days
└── → Expenses (مفلترة)
```

Add Expense **لا تكون تبويبًا**؛ FAB عائم في Home (وفي Expenses) لأنها العملية الأكثر تكرارًا.

---

## E. Data Model

Schema منطقي — بدون SQL تنفيذي. الأنواع المذكورة مفاهيمية (تُترجم عند implementation). **المبالغ تُخزَّن integer minor units دائمًا** (انظر G.1). التوقيتات (`created_at`/`updated_at`/`deleted_at`/`spent_at`/`purchased_at`...) تُخزَّن **UTC** (epoch milliseconds integer — K.5 محسوم) وتُحوَّل للعرض إلى المنطقة الزمنية المحلية في طبقة Presentation. **استثناء مقصود:** `start_date`/`end_date` للدورة **تواريخ تقويمية محلية** لا UTC instants — حدود الميزانية مرتبطة بيوم المستخدم المحلي (G.3).

### E.0 قواعد عامة

- **PK:** UUID (نص **v7** — زمني الترتيب: إدراج تسلسلي في فهارس SQLite وفائدة ترتيب الإنشاء للمزامنة والفحص مستقبلًا) لكل الجداول Sync-ready. لا اعتماد على auto-increment. **تحذير مقصود:** ترتيب v7 يعكس وقت *إنشاء السجل* لا وقت العملية — الاستعلامات الدومينية ترتّب بتوقيتاتها الصريحة (`spent_at`...) ولا ترتّب بالـID أبدًا.
- **Timestamps لكل الجداول:** `created_at` (لا يتغير)، `updated_at` (يتغير مع كل تعديل).
- **Soft delete** (`deleted_at NULL = نشط`) لكل الجداول Sync-ready. Settings بدون soft delete (تسطيح: مفتاح/قيمة).
- **لا بيانات مشتقة مخزنة** ما دامت تُحسب موثوقًا: لا أعمدة `spent_total` حية في budget_cycles، ولا `estimated_total` في shopping_lists، ولا `estimated_amount` في عناصرها (تُحسب من العناصر). الاستثناءان المقصودان: (1) `expenses.amount` — قيمة اعتمدها المستخدم عند الإدخال (مباشرة أو بعد quantity × unit price مع إمكانية تجاوز يدوي) وتُثبَّت عند الحفظ؛ (2) أعمدة لقطة إغلاق الدورة `final_expense_total` / `final_remaining` — قيمة اعتمدها النظام لحظة الإغلاق ولا يُعاد حسابها أبدًا (G.5).

### E.1 `expenses`

| الحقل | النوع | Nullable | ملاحظات |
|---|---|---|---|
| id | UUID | no | PK |
| name | text | no | اسم الشراء |
| amount | integer | no | minor units — القيمة النهائية المعتمدة |
| quantity | integer (scaled ×1000) | yes | NULL = لم تُدخل (تُعرض كـ 1) — مغاير عمدًا لافتراضي 1 في عناصر القوائم: غياب الإدخال ليس تأكيدًا لكمية؛ التمثيل: G.1 |
| unit_price | integer | yes | minor units؛ NULL إذا أُدخل amount مباشرة |
| category_id | UUID | yes | FK → categories؛ NULL = غير مصنّف |
| group_id | UUID | yes | FK → expense_groups؛ NULL = مصروف مفرد (مجموعات الشراء) |
| note | text | yes | |
| spent_at | datetime | no | وقت/tاريخ العملية **الفعلي** (قد يكون ماضيًا) — يختلف عن created_at |
| expense_source | enum(text) | no | `manual` / `shopping_list` — يفيد التتبع والإحصاء |
| created_at / updated_at / deleted_at | datetime | no/no/yes | |

**Indexes:** `spent_at` (كل الاستعلامات الزمنية)، `category_id`، `group_id`، `(spent_at, deleted_at)` مركّب للاستعلام الرئيسي «النشط خلال فترة».

### E.1bis `expense_groups` (مجموعات الشراء — أُضيفت بموافقة المالك بعد MVP الأساسي)

| الحقل | النوع | Nullable | ملاحظات |
|---|---|---|---|
| id | UUID | no | PK |
| name | text | no | اسم المجموعة/المحل |
| created_at / updated_at / deleted_at | | | |

- البنود أعضاء فيها عبر `expenses.group_id`؛ **عدد البنود غير محدود** (2، 3، 5، أكثر).
- الإجمالي مشتق دائمًا من البنود النشطة ولا يُخزَّن أبدًا (قاعدة E.0).
- العرض: بطاقة واحدة قابلة للطي تحمل الاسم + عدد البنود + الإجمالي؛ الحذف Swipe يحذف البنود (tombstones).
- الميزانية والإحصاءات لا تتغير: كل بند مصروف مستقل يُجمع مرة واحدة.

### E.2 `categories`

| الحقل | النوع | Nullable | ملاحظات |
|---|---|---|---|
| id | UUID | no | PK |
| name | text | no | اسم قابِل للترجمة للافتراضية (انظر H.4) |
| sort_order | integer | no | ثبات الترتيب |
| created_at / updated_at / deleted_at | | | |

**Indexes:** unique partial على `name` بين السجلات النشطة (مع soft delete لا يمكن unique مطلق).

التصنيفات الافتراضية السبع (Food, Home, Transportation, Bills, Health, Entertainment, Other) تُزرع عند أول تشغيل عبر migration، بأسماء مفاتيح ترجمة لا نصوص ثابتة.

### E.3 `budget_cycles`

| الحقل | النوع | Nullable | ملاحظات |
|---|---|---|---|
| id | UUID | no | PK |
| start_date | date | no | تاريخ فعلي لبداية الدورة |
| end_date | date | no | تاريخ فعلي للنهاية (يُحسب عند الإنشاء ويُثبَّت) |
| initial_amount | integer | no | المبلغ الابتدائي للدورة (بدون carry-over) |
| carry_over_amount | integer | no | المبلغ المُرحَّل من الدورة السابقة (0 إذا لا شيء؛ يمكن أن يكون سالبًا — K.3) |
| carry_over_enabled | boolean | no | الخيار المعتمد لهذه الدورة |
| start_day | integer | no | يوم بداية الدورة المعتاد (1–28/31) — يُورَّث للدورة التالية |
| previous_cycle_id | UUID | yes | FK ذاتي → يمكّن تتبع السلسلة والتدقيق |
| closed_at | datetime | yes | يُملأ مرة واحدة عند الإغلاق؛ NULL = دورة مفتوحة (G.5) |
| final_expense_total | integer | yes | لقطة الإغلاق — لا يُعاد حسابها أبدًا (G.5) |
| final_remaining | integer | yes | لقطة الإغلاق — أساس الترحيل؛ لا يُعاد حسابها (G.5) |
| created_at / updated_at / deleted_at | | | |

**Indexes:** `end_date`، وunique partial على `start_date` بين النشطة. ملاحظة: الفهرس يمنع تكرار تاريخ البداية فقط — عدم تداخل الدورات يضمنه منطق CycleResolver (الإنشاء الحصري عند الإغلاق)، لا الفهرس وحده.

المصفوفة الفعلية للدورة الجارية = `initial_amount + carry_over_amount` (لا حقل derived إضافي). الدورة المغلقة تُعرض من لقطة `final_*` لا من أي إعادة حساب.

### E.4 `shopping_lists`

| الحقل | النوع | Nullable | ملاحظات |
|---|---|---|---|
| id | UUID | no | PK |
| name | text | no | |
| archived_at | datetime | yes | أرشفة **يدوية فقط** (K.7)، منفصلة تمامًا عن soft delete |
| created_at / updated_at / deleted_at | | | |

### E.5 `shopping_list_items`

| الحقل | النوع | Nullable | ملاحظات |
|---|---|---|---|
| id | UUID | no | PK |
| shopping_list_id | UUID | no | FK → shopping_lists (ON DELETE: منع حذف أب نشط له عناصر نشطة) |
| name | text | no | |
| quantity | integer (scaled ×1000) | no | كمية ≥ 1، كسور حتى 3 خانات عشرية؛ التمثيل الكامل: G.1 |
| estimated_unit_price | integer | yes | minor units؛ قد يكون NULL (عنصر بلا تقدير) |
| note | text | yes | |
| purchased | boolean | no | افتراضي false |
| purchased_at | datetime | yes | يُملأ عند الشراء |
| expense_id | UUID | yes | FK → expenses؛ ربط تتبّعي بعد التحويل |
| actual_amount | integer | yes | السعر الفعلي المدخل عند الشراء (قبل/بعد إنشاء Expense) |
| created_at / updated_at / deleted_at | | | |

**قاعدتا Domain لحقول المال في هذا الجدول (محسومتان):**
- **Source of Truth للمبلغ الفعلي:** عندما يكون `expense_id` مرتبطًا بمصروف **نشط** — والنشط مُعرَّف حصريًا: `deleted_at IS NULL` — فإن `expense.amount` هو المرجع الوحيد للعرض والتجميع؛ و`actual_amount` يُستخدم فقط عند غياب مصروف مرتبط نشط (شراء دون تحويل). إن حُذف المصروف المرتبط لاحقًا، يعود العرض تلقائيًا إلى `actual_amount`. وتعديل المصروف المرتبط لا يُعيد كتابة `actual_amount` (يبقى كما أُدخل أصلًا).
- **قيمة السطر التقديرية لا تُخزَّن:** حُذف عمود `estimated_amount` — تُحسب عند العرض بالقاعدة الحتمية نفسها: تقريب نصف-أعلى لـ `quantity × estimated_unit_price` (G.2 / G.6). لا يوجد مسار «تجاوز يدوي» للقيمة التقديرية في التدفقات، ولا سلسلة تجميد تعتمد عليها، فلا مبرر لاستثناء تخزيني.

**Indexes:** `shopping_list_id`، `expense_id`.

### E.6 `settings`

جدول/مخزن مفتاح-قيمة (ليس Sync-ready في MVP):

| المفتاح | القيمة |
|---|---|
| language | `ar` / `en` / `fr` |
| currency_code | ISO 4217 (مثلاً `TND`) |
| budget_start_day | 1–31 |
| budget_carry_over | boolean |
| budget_default_amount | integer minor units — **Default budget amount** لدورات المستقبل؛ مستقل عن مبلغ الدورة الحالية الذي يعيش في صف budget_cycles (G.5) |
| budget_enabled | boolean |
| first_run_completed | boolean |

ملاحظة: إعدادات Budget هنا هي «القالب»؛ الدورة الفعلية تعيش في `budget_cycles`. هذا يفصل قرار المستخدم عن تنفيذ الدورة (انظر K.2).

### E.7 علاقات المفاتيح الأجنبية

- `expenses.category_id → categories.id` (SET NULL عند حذف التصنيف — soft delete يجعل ذلك نادرًا).
- `shopping_list_items.shopping_list_id → shopping_lists.id` (RESTRICT).
- `shopping_list_items.expense_id → expenses.id` (SET NULL).
- `budget_cycles.previous_cycle_id → budget_cycles.id` (SET NULL).

**ملاحظة على أفعال FK (توضيح للـimplementation):** لأن جميع الكيانات Sync-ready تعتمد **soft delete** (لا حذفًا فيزيائيًا)، فإن أفعال FK أعلاه (SET NULL / RESTRICT) **نظرية/احتياطية** — لن تُفعَّل في الممارسة. القواعد الفعلية (مثل «expense نشط = `deleted_at IS NULL`» في E.5) تُنفَّذ على مستوى التطبيق/Domain، لا على مستوى قيود قاعدة البيانات. يُثبَّت الـFK للسلامة المرجعية والاتساق مع نمط المزامنة المستقبلي، ويجب ألا يعتمد منطق الأعمال عليه.

---

## F. Architecture

### F.1 القرار

**Decision:** Architecture الطبقات مع Riverpod كطبقة state management.

- **Options:** (1) MVVM + Repository مع Provider. (2) Clean Architecture كاملة (UseCases منفصلة) مع BLoC. (3) Layered + Riverpod (Application/State + Domain ضمن نفس المشروع، بدون over-engineering).
- **Recommendation:** (3) — الطبقات كما في الدستور، مع Riverpod للـ state management وdrift لطبقة SQLite.
- **Reason:** التطبيق متوسط الحجم: UseCases منفصلة لكل عملية ستضيف كراتين كود بلا قيمة الآن؛ يكفي وضع منطق الأعمال في طبقة Domain Services قابلة للاختبار (BudgetCalculator, CycleResolver, ShoppingConverter). Riverpod يوفر اختبارًا سهلًا، وProviderScope يسمح بحقن Repositories بلا code-generation إلزامي.

### F.2 الطبقات

```text
Presentation (Screens/Widgets)
    ↓ تستدعي
Application / State (Riverpod controllers/notifiers)
    ↓ تستدعي
Domain / Business Logic (خدمات صرفة بلا Flutter ولا SQLite:
  MoneyMath, BudgetCalculator, CycleResolver, ShoppingConverter)
    ↓ تستخدم
Repository interfaces (contracts مجردة)
    ↓ تُنفَّذ بواسطة
Local Data Source (drift/SQLite)
```

مستقبلًا: تُضاف تنفيذات Repository ثانية أو Sync Layer بجانب Local Data Source **دون تغيير** Presentation أو Application — هذا هو جوهر اختيار Repository pattern.

### F.3 مكتبة SQLite

**Decision:** `drift` مقابل `sqflite` الخام.
- **Options:** (1) `sqflite` + SQL يدوي + migrations يدوية. (2) `drift` — queries موصولة بالنوع، migrations إصدارية مدمجة، قابلية اختبار عالية.
- **Recommendation:** `drift`.
- **Reason:** الدستور يفرض «explicit, versioned migrations محفوظة لبيانات المستخدم» — drift يعطيها آليًا ويجعلها قابلة للاختبار، ويقلل أخطاء SQL اليدوية. تكلفة التعلم مقبولة مقابل أمان البيانات المالية.

### F.4 قواعد التبعية

- Presentation لا تستورد أي شيء من Data Source (ممنوع UI → SQLite — القسم 28 من الدستور).
- Domain لا تستورد Flutter ولا drift — كود Dart صرف قابل للاختبار بالوحدات.
- Models مشتركة بسيطة (immutable) تُمرر بين الطبقات؛ كيانات drift لا تخرج من طبقة Data Source.

---

## G. Money Calculation Rules

### G.1 تمثيل المال

- **integer minor units** حصريًا في التخزين والحساب (مثال: 12.50 TND = 1250).
- كل عملة لها `minor_unit_digits` من ISO 4217 (معظمها 2؛ الدينار التونسي بالمليم = 3 خانات؛ الين 0) — لا فرض عالمي لخانتين، ولا تصميم حول EUR/USD يُلاقى لاحقًا (K.6 محسوم).
- لا floating point في أي حساب مالي. الإدخال النصي يُحوَّل إلى minor units بدقة (تحليل عشري، لا `double.parse`).
- التقريب الوحيد المسموح: عند **العرض** فقط (طبقة Presentation)، وبأقل عدد خانات ممكن.
- `MoneyMath` = دوال جمع/طرح/ضرب صحيحة على minor units — المصدر الوحيد لأي عملية حسابية مالية.

**تمثيل Quantity (مستقل عن المال — محسوم):**
- الكمية تسمح بالكسور (1.5 كغ) وتُمثل **scaled integer بثلاث خانات عشرية ثابتة** (القيمة × 1000) — لا floating point، ولا ارتباط بدقة العملة.
- لا يوجد حقل «وحدة» في MVP؛ دلالة الوحدة (كغ/حبة/لتر) تعيش في اسم العنصر أو الملاحظة، وبالتالي لا دقة لكل وحدة.
- النطاق: أكبر من صفر، بحد أعلى معقول (تحقق عند الإدخال).
- حاصل `quantity × unit_price` يخضع لقاعدة التقريب الوحيدة (G.2) إذا كسر دقة minor units.

### G.2 Amount (المصروف)

- إدخال مباشر: `amount = القيمة المدخلة`.
- من Quantity × Unit Price: `amount = quantity × unit_price` بالضبط في minor units. النتيجة دائمًا صحيحة إذا كان كلاهما بنفس دقة العملة؛ إذا أدخل المستخدم كسورًا أدق من وحدة العملة، **يُقرَّب مرة واحدة عند الحفظ** (نصف لأعلى، half-up) ويُثبَّت — يظهر للمستخدم المبلغ المثبّت قبل الحفظ.
- إذا عدّل المستخدم amount يدويًا بعد الحساب التلقائي → المدخل هو القيمة النهائية (G لا تُعيد الحساب فوقه).

### G.3 Budget Cycle Boundaries

- الدورة: من `start_date` إلى `end_date` حيث `end_date = (start_date + شهر) - يوم واحد` بتقويم فعلي:
  - start = 15 يناير → end = 14 فبراير. start = 25 يناير → end = 24 فبراير. start = 1 فبراير → end = 28/29 فبراير.
- **قاعدة start day عند نهايات الشهور (29–31):** يُثبَّت فعليًا إلى آخر يوم من الشهر (start day 31 في فبراير → تبدأ 28/29). `start_day` المخزن يبقى 31 (نوايا المستخدم)، والحساب يقلّصه حسب الشهر. هذا القرار محسوم كافتراضي (سلوك «clamp» القياسي) لكنه يُذكر هنا ليُختبر صراحة.
- **التواريخ التقويمية المحلية (K.5 محسوم):** `start_date`/`end_date` تواريخ تقويمية في يوم المستخدم المحلي — ليست UTC instants. نافذة الإنفاق `[start_date 00:00, end_date 23:59:59]` تُحسب بحدود اليوم المحلي وتُحوَّل لحظة الاستعلام إلى UTC. تغيّر المنطقة الزمنية للجهاز قد يحرّك انتماء اللحظات الحدية بين دورتين — سلوك مقبول وموثق.
- «الدورة الحالية» في تاريخ معين = الدورة النشطة التي `start_date ≤ today ≤ end_date`.
- Spent خلال الدورة = مجموع `expenses.amount` حيث `spent_at` ضمن `[start_date 00:00, end_date 23:59:59]` محليًا و`deleted_at IS NULL`.
- **نقطة حساسة:** مصروف في يوم انتقالي يُنسب للدورة التي يحتوي يومه فعليًا — لا يُنسب للدورة «الشهرية» (تُختبر حدود: 14/15، 24/25، آخر/أول شهر).

### G.4 Remaining & Daily Available

- `Remaining = (initial_amount + carry_over_amount) − spent_in_cycle` — قد تكون سالبة (يُعرض التحذير بوضوح، بلا إخفاء).
- `Remaining days` = عدد الأيام من اليوم حتى `end_date` شاملًا (اليوم الحالي يُحتسب يوم إنفاق متاح).
- `Daily available = Remaining / Remaining days` (division صحيح بعرض عشري واحد/اثنين). داخل الدورة الجارية لا تقل الأيام المتبقية عن 1 (اليوم محسوب)؛ خارج الدورة (لحظةً بين انتهاء دورة وإنشاء التالية) يُعرض «—» بدل القسمة على صفر.
- **إرشادي فقط:** لا حجب، لا تنبيه إلزامي، لا حد إنفاق. يظهر في Home كمؤشر نصي/بطاقة بسيطة.
- Average daily spending (إحصائية) = spent خلال الفترة ÷ عدد أيام الفترة **المنقضية** (وليس كامل الفترة).

### G.5 إغلاق الدورات، الترحيل، والتجميد

**قواعد الإغلاق (Cycle Closing):**
- الإغلاق يحدث عند أول فتح للتطبيق بعد تجاوز `end_date` (أو فورًا عند «ابدأ دورة جديدة الآن» اليدوي).
- عند الإغلاق تُخزَّن في صف الدورة **لقطة كاملة (Snapshot)** تُكتب مرة واحدة ولا يُعاد حسابها أبدًا:
  - `final_expense_total` — مجموع مصروفات الدورة كما اعتبره النظام لحظة الإغلاق.
  - `final_remaining` — المتبقي النهائي (أساس الترحيل المحتمل).
  - `closed_at` — لحظة الإغلاق الفعلية (قد تتأخر عن `end_date` إن لم يُفتح التطبيق؛ ليست مرادفًا لها).
- اللقطة استثناء مقصود من قاعدة «لا بيانات مشتقة» (E.0) — من فئة `expenses.amount`: قيمة اعتُمدت في لحظة، وهدفها استقرار التاريخ وسلسلة الترحيل.

**الترحيل (Carry-over) — محسوم (K.10):**
- `carry_over_amount(new) = final_remaining(last closed)` إذا كان الترحيل مفعّلًا في الدورة الجديدة، وإلا `0`.
- **الفجوات:** إذا تجاوز غياب المستخدم دورة كاملة أو أكثر، يُرحَّل **مباشرة من آخر دورة مغلقة إلى الدورة الحالية** — الفترات بلا دورات «لم توجد أصلًا» ولا تُنشأ لها دورات فارغة بأثر رجعي. مثال: آخر دورة مغلقة انتهت بمتبقٍ 300 وغاب المستخدم شهرين → الدورة الحالية تبدأ بالمبلغ الافتراضي + 300.
- الترحيل السالب: كما هو عند تفعيل الترحيل (C.7).
- مثال الدستور: initial 1500، spent 1200 → `final_remaining` 300 → الدورة الجديدة: 1500 + 300 = **1800 متاح**.

**التعديل بعد الإغلاق — محسوم (K.8):**
- تعديل/حذف مصروف ينتمي لدورة **مغلقة** لا يعيد بناء لقطة الدورة ولا الترحيل التاريخي — يؤثر فقط في الإحصاءات الحية (تُحسب دائمًا من جدول expenses لا من اللقطات).
- النتيجة المتوقعة والمقبولة: قد تختلف «إحصائية دورة مغلقة محسوبة الآن» عن «لقطة الدورة المغلقة» بعد تعديلات لاحقة — هذا تعريف لا خلل: شاشات تاريخ الدورات تعرض اللقطة، وشاشات الإحصاءات تعرض الحساب الحي.

**فصل مبلغ الميزانية — محسوم (K.9):**
- **Current cycle amount** = `budget_cycles.initial_amount` للدورة الجارية؛ تعديله يسري فورًا على هذه الدورة فقط.
- **Default budget amount** = إعداد مستقل (E.6) لدورات المستقبل؛ يُغيَّر فقط من Budget Settings.
- تعديل الحالي لا يغيّر الافتراضي؛ شاشة التعديل توضح النطاق بسطر صغير («هذا التعديل للدورة الحالية فقط»).

### G.6 Shopping List Estimated Total

- `estimated_total = Σ round(quantity × estimated_unit_price)` للعناصر غير المحذوفة وغير Purchased — يُحسب عند العرض بقاعدة التقريب الموحدة (G.2)، ولا يُخزَّن أي عمود مشتق.
- عنصر بلا estimated_unit_price يساهم بصفر في المجموع، وتظهر القائمة إشارة «2 عناصر بلا تقدير» لتفادي وهم دقة المجموع.
- المقارنة مع Remaining تُعرض كنسبة/نص، وتُحسب في طبقة Domain بـ minor units.

---

## H. Localization Strategy

### H.1 الإطار

- Flutter official `gen_l10n` (ARB files) — `app_ar.arb`, `app_en.arb`, `app_fr.arb`.
- **ممنوع أي نص ثابت في الكود** (القسم 28 من الدستور). Lint يمنع النصوص الحرفية في Widgets.
- كل النصوص، بما فيها أسماء التصنيفات الافتراضية وأسماء أيام الأسبوع في الإحصائيات، من resources.

### H.2 RTL/LTR

- العربية: `Directionality` RTL تلقائي مع `locale=ar`؛ استخدام Flutter `Intl` والـ `textDirection` الوعي بالاتجاه.
- تصميم قاوم الاتجاه: لا أرقام هوية موضعية، استخدام `EdgeInsetsDirectional`/`start/end` بدل left/right، الأيقونات الاتجاهية (رجوع/تقديم) تُعكس تلقائيًا.
- المبالغ والتواريخ **لا تُعكس** داخل RTL (استخدام تنسيق أرقام محايد اتجاهيًا).
- خط يدعم العربية جيد (اختبار Noto Sans Arabic أو افتراضي النظام) — يُقرر عند implementation.

### H.3 فصل اللغة عن العملة

- `locale` و`currency_code` إعدادات مستقلة تمامًا (E.6). تغيير اللغة لا يلمس العملة والعكس.
- تنسيق المبالغ: نمط العملة (رمز/موضع الرمز/الفواصل) يتبع **العملة**، ولغة الحروف تتبع **الواجهة** (مثال: واجهة عربية + عملة EUR → «1 234,50 €» بصياغة معقولة ثنائية الاتجاه).
- التواريخ تتبع الواجهة (locale)، والمدة (الأيام المتبقية) نص مترجم مع pluralization صحيح (ARB plurals) لثلاث لغات.

### H.4 التصنيفات الافتراضية

تُخزن في DB كمفاتيح (`cat.food` إلخ) — الاسم المعروض يُترجم حسب اللغة الحالية. التصنيفات التي ينشئها المستخدم نص خام بلا ترجمة.

---

## I. Future Cloud Sync Strategy

التنفيذ مؤجل بالكامل؛ هذا قسم تصميم استباقي فقط.

### I.1 ما يجعله ممكنًا لاحقًا (مُنجز في MVP)

- UUIDs + `created_at`/`updated_at`/`deleted_at` في كل كيان قابل للمزامنة → كل سجل يحمل هوية وهوية زمنية مستقلة عن الجهاز.
- Soft delete (tombstones) → الحذف قابل للمزامنة.
- Repository interfaces → طبقة Application لا تعرف مصدر البيانات؛ إضافة Sync Layer لا تغيّر Presentation.
- تخزين التوقيتات بصيغة قابلة للمقارنة (K.5) → لا اعتماد على ساعة الجهاز في المنطق.

### I.2 ما سيُطلب مستقبلًا (لا يُنفَّذ الآن)

| المتطلب | الفكرة |
|---|---|
| Sync status | عمود/جدول `sync_state` (per record أو per entity): synced / pending / conflict |
| Offline queue | جدول outbox يسجل العمليات المعلقة بترتيب حدوثها |
| Conflict detection | مقارنة `updated_at` المحلي مع نسخة الخادم (server version/etag) |
| Conflict resolution | سياسة معلنة: last-writer-wins افتراضيًا، مع كشف التضارب البنيوي (مثلاً تعديل قائمة محذوفة) وحوار للمستخدم في الحالات المالية الحساسة |
| Last sync timestamp | per-device، في settings المحلية |
| الخادم | أي Backend (Supabase/Firebase/Custom) — الـ Repository يعزل عنه التطبيق |
| الأمان | تشفير أثناء النقل، وتحديد نطاق ما يُزامَن (ربما الإعدادات أولاً ثم السجلات) — يُصمم ويُفصح عنه قبل التنفيذ (التزام الدستور بالخصوصية) |

### I.3 قراءة سببية واحدة

قرار MVP الحاسم: **كل كتابة تمر عبر Repository وتُثبّت `updated_at`** — إذا التزمت الطبقات بهذا اليوم، فإن إضافة Sync لاحقًا تكون «إضافة تنفيذ» لا «إعادة بناء».

---

## J. Risks

| # | الخطر | التصنيف | التخفيف |
|---|---|---|---|
| 1 | أخطاء تقريب/تمثيل المال (كسور، دقة الدينار التونسي 3 أعشار) | تقني - حرج | integer minor units حصريًا + MoneyMath مركزي + اختبارات وحدة مكثفة (G.1) |
| 2 | حدود الدورة (أشهر 28/29/30/31 + start days نهاية الشهر) تنتج دورات متداخلة أو فارغة | تقني | CycleResolver بمنطق clamp + جدول اختبارات حدود صريح (G.3) |
| 3 | Carry-over السالب يعطي دورة «مديونة» مربكة | منتجي | قرار واضح (K.3) + عرض صريح لا يخفي السلب |
| 4 | تعقيد تدفق التحويل (قائمة → مصروف) يقتل السرعة | منتجي | سؤال واحد فقط عند الشراء (C.10) + افتراضات ذكية + Undo |
| 5 | فقدان بيانات محلية (حذف التطبيق، تلف الجهاز) — لا نسخ احتياطي في MVP | منتجي/تقني | Export مؤجل لكنه أول ميزة بعد MVP؛ توضيح مسؤولية النسخ في Privacy screen |
| 6 | drift/code-gen يعقّد البناء للوافد الجديد | تقني | عزل طبقة Data Source + توثيق pattern واحد يُحاكى |
| 7 | RTL يعطي تخطيطات مكسورة في شاشات الإدخال الرقمية | تقني | لوحة أرقام عشرية موحدة + مراجعة RTL لكل شاشة (H.2) |
| 8 | نمو «الإحصائيات» إلى تحليل مالي معقد بالتسارع | منتجي | الالتزام بقائمة B.1 فقط؛ أي إضافة تمر على قواعد القسم 26 من الدستور |
| 9 | توقيتات الجهاز الخاطئة (ساعة/منطقة) تشوّه التقارير والدورات | تقني | تخزين UTC + عرض محلي + عدم الاعتماد على ساعة الجهاز في مقارنات المزامنة مستقبلًا |
| 10 | تباين محتمل بين لقطة دورة مغلقة والإحصاءات الحية بعد تعديل مصروفات قديمة | تقني/منتجي | قاعدة صريحة (G.5): تاريخ الدورات يعرض اللقطة والإحصاءات تحسب حيًا؛ اختبارات تغطي التباين المتعمد |

---

## K. Open Questions — يجب حسمها قبل implementation

> جميع الأسئلة K.1–K.11 **محسومة الآن** (آخر جولة تصديق) وتُبقى هنا كقرارات موثقة. أي سؤال جديد يظهر أثناء التنفيذ يُصعَّد لمالك المنتج ولا يُحسم بصمت.

### K.1 العملة الافتراضية عند أول تشغيل؟ — **محسوم**
**القرار:** لا افتراض مبني على اللغة أو الموقع إطلاقًا. يطلب التطبيق من المستخدم اختيار العملة صراحةً عند أول تشغيل (خطوة واحدة غير قابلة للتخطي)، وتغييرها لاحقًا من Settings.

### K.2 تعديل الميزانية في منتصف الدورة؟ — **محسوم**
**القرار:** تعديل initial amount يسري فورًا على الدورة الحالية فقط (ولا يغيّر الافتراضي — K.9)؛ تغيير start day يسري من الدورة التالية فقط.

### K.3 Carry-over سالب؟ — **محسوم**
**القرار:** يُرحَّل كما هو مع ترحيل مفعّل (صدق مالي)؛ ويُتجاهل مع ترحيل معطّل (بداية نظيفة). لا اقتطاع صامت ولا حجب إنفاق.

### K.4 التحويل الجماعي لعناصر القائمة؟ — **محسوم**
**القرار:** لا يوجد Bulk conversion في MVP. التحويل عنصرًا بعنصر فقط — كل عنصر يحتاج سعره الفعلي، وبعض العناصر قد لا تُشترى.

### K.5 صيغة تخزين التوقيتات؟ — **محسوم**
**القرار:** كل التوقيتات تُخزَّن UTC (epoch milliseconds integer) وتُعرض بالتحويل إلى المنطقة الزمنية المحلية في الـUI. الاستثناء المقصود: `start_date`/`end_date` للدورة **تواريخ تقويمية محلية** (ليست UTC instants) لأن حدود الميزانية مرتبطة بيوم المستخدم المحلي — قاعدة Domain صريحة في G.3.

### K.6 دقة الدينار التونسي (millimes) والعملات غير القياسية؟ — **محسوم**
**القرار:** minor units عامة لكل عملة وفق ISO 4217 — TND بالمليم (1/1000، ثلاث خانات). لا فرض خانتين عالميًا، ولا تصميم حول EUR/USD يُلاقى لاحقًا.

### K.7 أرشفة القوائم المكتملة؟ — **محسوم**
**القرار:** لا أرشفة تلقائية عند الاكتمال — القائمة تبقى ظاهرة، والأرشفة/الحذف يدويان. `archived_at` مفهوم منفصل عن `deleted_at` (أرشيف ≠ حذف منطقي) ويبقى في MVP لأن الأرشفة اليدوية مدعومة.

### K.8 تعديل مصروف في دورة مغلقة — ما أثره بالضبط؟ — **محسوم**
**القرار:** يؤثر فقط في الإحصاءات الحية؛ لا يعيد بناء اللقطة ولا الترحيل التاريخي (G.5).

### K.9 هل تعديل مبلغ الدورة الحالية يغيّر Default Budget Amount؟ — **محسوم**
**القرار:** لا — فصل صريح بين Current cycle amount وDefault budget amount؛ تغيير الافتراضي من Budget Settings فقط.

### K.10 قاعدة الترحيل عند القفز فوق دورة أو أكثر؟ — **محسوم**
**القرار:** ترحيل مباشر من آخر دورة مغلقة إلى الدورة الحالية؛ الفترات بلا دورات «لم توجد أصلًا» (G.5).

### K.11 «ابدأ من اليوم» عند أول تفعيل — دورة جزئية أم شهر كامل؟ — **محسوم**
**القرار:** دورة جزئية من اليوم حتى نهاية الفترة الحالية طبيعيًا (تفعيل 12 سبتمبر وstart day = 1 → دورة 12–30 سبتمبر)، ثم يعود إيقاع start day من الدورة التالية.

---

## L. Recommended Development Phases

وصف مرحلي فقط — **لا تنفيذ في هذه الوثيقة**. كل مرحلة تنتهي بحالة تعمل واختباراتها.

### Phase 0 — Project Foundation
هيكل الطبقات والمجلدات، drift setup، migrations v1، CI بسيط، l10n scaffolding (AR/EN/FR)، إعدادات اللغة/العملة/الثيم. *معيار الخروج:* تشغيل + تغيير اللغة يعمل + DB تُنشأ وتُهاجر.

### Phase 1 — Core Expenses
CRUD للمصروفات، Quick Add flow، soft delete، حقول quantity/unit_price، مع MoneyMath واختباراته، Expenses list بالبحث والفلاتر، Home الأساسي (بدون ميزانية).

### Phase 2 — Categories & Statistics
Categories (زرع الافتراضية + إدارة)، ربطها بالمصروفات، Statistics بشرائحها وأرقامها.

### Phase 3 — Budget Cycles
Budget screen، CycleResolver، الحسابات (G.3–G.4)، بطاقة Budget في Home، اختبارات حدود الشهور المكثفة.

### Phase 4 — Carry Over & Cycle Lifecycle
الترحيل، الإنشاء الكسول للدورات (lazy materialization)، قواعد الإغلاق واللقطة (`final_*`، `closed_at`)، الفجوات، التعامل مع السالب، سلسلة previous_cycle_id.

### Phase 5 — Shopping Lists
قوائم وعناصر، Estimated total، تدفق الشراء والتحويل (C.9–C.10)، الأرشفة.

### Phase 6 — Polish & Hardening
RTL audit كامل، مراجعة نصوص الترجمة الثلاثية، معالجة الأخطاء الظاهرة للمستخدم، Privacy screen، مسح البيانات، مراجعة أداء الاستعلامات والفهارس.

### Phase 7 (Post-MVP) — Data Portability
Export JSON/CSV، Import/restore — تمهيدًا لأي مزامنة مستقبلية.

---

## خلاصة القرارات الموصى بها في هذه الوثيقة

| القرار | التوصية |
|---|---|
| State management | Riverpod |
| طبقة SQLite | drift |
| دورة الميزانية | إنشاء كسول: الدورة الحالية تُحسب، والصف يُخزَّن فقط عند الإغلاق؛ لا صفوف مستقبلية ولا دورات فارغة للفجوات |
| إغلاق الدورة | لقطة كاملة عند الإغلاق (`final_expense_total`، `final_remaining`، `closed_at`) لا يُعاد حسابها؛ التعديل اللاحق يصيب الإحصاءات فقط (K.8) |
| مبلغ الميزانية | فصل Current cycle amount عن Default budget amount؛ تعديل الحالي لا يغيّر الافتراضي (K.9) |
| Carry-over سالب | يُرحَّل كما هو مع ترحيل مفعّل؛ ويُتجاهل مع معطّل |
| المبلغ الفعلي لعنصر مشترى | `expense.amount` هو Source of Truth عند وجود مصروف نشط مرتبط؛ `actual_amount` عند غيابه (E.5) |
| القيمة التقديرية للعنصر | لا تُخزَّن — حساب حي بتقريب حتمي من `quantity × estimated_unit_price` |
| Quantity | كسور حتى 3 خانات، scaled integer (×1000)، بلا حقل وحدة في MVP، مستقلة عن دقة العملة |
| العملة عند أول تشغيل | اختيار صريح من المستخدم دائمًا — لا استنتاج من اللغة أو الموقع |
| تحويل القائمة | عنصرًا بعنصر فقط — لا Bulk conversion في MVP |
| التوقيتات | UTC (epoch ms) مع عرض محلي؛ حدود الدورة تواريخ تقويمية محلية |
| أرشفة القوائم | يدوية فقط؛ `archived_at` منفصل عن `deleted_at` |
| Navigation | 4 تبويبات + Budget/Settings شاشات ثانوية |
| المال | integer minor units + minor_unit_digits لكل عملة (ISO 4217) |
| Sync readiness | UUID + timestamps + soft delete + Repository interfaces — مُلزمة من اليوم الأول |
